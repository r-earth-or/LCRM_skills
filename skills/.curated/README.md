# Curated Skills

`skills/.curated` 下每个子目录都是可被 Skills CLI 直接安装的独立 skill。

## Skills

- `lcrm-search`
- `lcrm-business-record`
- `lcrm-presales-itinerary`

## 约束

- 每个目录根下必须有 `SKILL.md`
- 目录内部必须自包含依赖文件（例如 `scripts/`）
- 不依赖同级或父级目录文件
